import { Route } from '@angular/router';
import { EndUserStorageComponent } from './end-user-storage.component';

export const routing: Route[] = [
  {
    path: '', component: EndUserStorageComponent
  }
];
