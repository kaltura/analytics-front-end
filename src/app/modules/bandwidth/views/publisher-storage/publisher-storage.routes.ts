import { Route } from '@angular/router';
import { PublisherStorageComponent } from './publisher-storage.component';

export const routing: Route[] = [
  {
    path: '', component: PublisherStorageComponent
  }
];
